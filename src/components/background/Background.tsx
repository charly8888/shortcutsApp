import styles from './Background.module.scss'
const Background = () => {
  return (
    <div className={styles.background}>
      <h5>Beta version, don't save anything that you don't permit it loss </h5>
    </div>
  )
}

export default Background
